/**
 * Shared class strings for form-field chrome (labels, helper/error text),
 * so every framework's Input/Select/Checkbox renders identical structure.
 * Semantic utilities only — themeable via tokens.
 */
export const fieldLabel = 'text-sm font-medium text-fg';
export const fieldHelp = 'text-sm text-fg-muted';
export const fieldError = 'text-sm text-danger';
export const checkboxLabel =
  'inline-flex items-center gap-2 cursor-pointer select-none text-fg';
